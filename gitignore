# Python
.venv/
__pycache__/
*.pyc
*.pyo

# Machine-local config — never commit
pipeline/pipeline.yaml

# Projects live outside the repo or locally — never commit
# project_template/ is NOT ignored: it is part of the repo
projects/
